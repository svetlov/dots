set -g prefix C-b
set -g status off
set -g status-position top
set -g status-style "bg=red,fg=white,bold"
set -g status-right ""
set -g window-status-format ""
set -g window-status-current-format ""
set -g status-format[0] "#[align=left]#{E:status-left}"
run-shell -b '$DOTS_HOME/all/tmux/tmux-claude-status.sh'
set -g history-limit 10000
set -g mouse on
set -g xterm-keys on
setw -g mode-keys vi

# set-environment -g 'SSH_AUTH_SOCK' ~/.ssh/ssh_auth_sock
# update SSH_* env from env parent env
set-option -g update-environment "SSH_AUTH_SOCK"
set-option -g update-environment "SSH_AGENT_PID"

# see https://github.com/neovim/neovim/issues/2035 fro details
set -sg escape-time 30
# see https://github.com/tmux/tmux/issues/193 for details
set -g assume-paste-time 10

# Use vi keybindings in copy and choice modes
bind-key b copy-mode
bind-key -T copy-mode-vi 'v' send-keys -X begin-selection

# we need to set it to support right colors in windows terminals
set -as terminal-features ",xterm-256color:RGB"

# allows terminal escape sequences to pass directly through tmux
# to your terminal emulator when the pane is visible, required
# for molten-nvim / image.nvim (for ipynb neovim extensions)
set -g allow-passthrough on

# Clear images when switching tmux windows (for image.nvim)
set -g visual-activity off

bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
bind -n WheelDownPane select-pane -t= \; send-keys -M
bind -n C-WheelUpPane select-pane -t= \; copy-mode -e \; send-keys -M
bind -T copy-mode-vi    C-WheelUpPane   send-keys -X halfpage-up
bind -T copy-mode-vi    C-WheelDownPane send-keys -X halfpage-down

unbind |
bind-key | split-window -h -c '#{pane_current_path}'
unbind _
bind-key _ split-window -v -c '#{pane_current_path}'


# Smart pane switching with awareness of vim splits
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key h if-shell "$is_vim" "send-keys C-h"  "select-pane -L"
bind-key j if-shell "$is_vim" "send-keys C-j"  "select-pane -D"
bind-key k if-shell "$is_vim" "send-keys C-k"  "select-pane -U"
bind-key l if-shell "$is_vim" "send-keys C-l"  "select-pane -R"

bind-key r command-prompt -p "Rename pane:" "set -p @custom_name '%%'"
bind-key w run-shell 'PATH="$HOME/.oh-my-zsh/custom/plugins/fzf/bin:$PATH" && $DOTS_HOME/all/tmux/tmux-window-list.sh "#{session_name}:#{window_index}" "$HOME/.oh-my-zsh/custom/plugins/fzf/bin/fzf-tmux"'
set -g pane-border-status top
set -g pane-border-format " #{pane_index}: #{@custom_name} | #{pane_title} | #{b:pane_current_path} @ #{pane_current_command} #(ps -o pid= --ppid #{pane_pid} | tail -1) "
bind-key \\ resize-pane -Z
bind-key = select-layout even-horizontal

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# Auto-restore on tmux server start
set -g @continuum-restore 'on'

# Auto-save on structural changes
set-hook -g window-linked 'run-shell -b "$DOTS_HOME/all/tmux/plugins/tmux-resurrect/scripts/save.sh quiet"'
set-hook -g window-unlinked 'run-shell -b "$DOTS_HOME/all/tmux/plugins/tmux-resurrect/scripts/save.sh quiet"'
set-hook -g session-created 'run-shell -b "$DOTS_HOME/all/tmux/plugins/tmux-resurrect/scripts/save.sh quiet"'

# for pbcopy/pbpaste
if-shell '[[ -n "$DOTS_HOME" ]] && uname | grep -q Linux' "source-file $DOTS_HOME/all/tmux/linux.conf"
if-shell '[[ -n "$DOTS_HOME" ]] && uname | grep -q Darwin' "source-file $DOTS_HOME/all/tmux/macos.conf"

# Initialize TPM (must be last line)
run '$DOTS_HOME/all/tmux/plugins/tpm/tpm'
