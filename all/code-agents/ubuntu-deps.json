{
  "packages": [
    {
      "name": "poppler-utils",
      "consumer": "paper-summarization",
      "purpose": "Provides pdftotext for extracting text when Claude cannot read large PDFs directly."
    },
    {
      "name": "pciutils",
      "consumer": "cuda checks",
      "purpose": "to get lspci command"
    },
    {
      "name": "trash-cli",
      "consumer": "shell safety",
      "purpose": "Move files to trash instead of permanent deletion, with recoverable restore and TTL-based cleanup."
    }
  ]
}
